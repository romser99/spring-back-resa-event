package fr.solutec.resaevent.services;

import fr.solutec.resaevent.entites.Equipe;

import java.util.ArrayList;
import java.util.List;

public class EquipeService {/*
    public Equipe create(Equipe equipe) {
        int id =(int)(Math.random() * 50 + 1);
        equipe.setId(id);
        return new Equipe(equipe.getPrenom(), equipe.getNom(),equipe.getEmail(),equipe.getTelephone(), equipe.getPassword(),null, id);

    }

    public List<Equipe> list() {
        List<Equipe> equipes = new ArrayList<>();
        for (int i=0; i<10; i++){
            Equipe equipier = new Equipe();
            equipier.setId(i);
            equipier.setNom("NOM"+ i);
            equipier.setPrenom("PRENOM"+ i);
            equipier.setEmail("EMAIL"+ i);
            equipier.setTelephone("TELEPHONE"+ i);
            equipes.add(equipier);

        }

        return equipes;
    }

    public Equipe read(Equipe nom) {
        List<Equipe> equipes = this.list();
        Equipe equipe = equipes.stream(nom);
        return equipe;
    }

    public void delete(int id){

    }*/
}
