package fr.solutec.resaevents.services;

import fr.solutec.resaevents.entites.Personne;

/*public class PersonneService {
    public Personne create(Personne personne) {
        Personne PersonneACreer = new Personne();
        PersonneACreer.setNom(personne.getNom());
        PersonneACreer.setPrenom(personne.getPrenom());
        PersonneACreer.setMdp(personne.getMdp());
        PersonneACreer.setMail(personne.getMail());
        PersonneACreer.setTelephone(personne.getTelephone());
        return PersonneACreer;
    }

    public void display(Personne PersonneACreer) {
        System.out.println(PersonneACreer.getNom() + PersonneACreer.getPrenom() + PersonneACreer.getMail()
        + PersonneACreer.getTelephone());
    }
} */
